Purpose: Read-only setup state for one project: registered sources, wired providers, hook installation per scope, service install state, global config health.

When: Determining whether `setup_apply` has been run, auditing configuration, confirming a setup change landed.

Prereq: None.

Args:
  - cwd: optional string. Project working directory. Defaults to the daemon's cwd.

Returns:
  result: {project, global_config_path, global_setup_applied, runtime_sources, providers, service_installed, issues}.

Errors: none expected.

Next: setup_plan to preview changes; setup_apply(yes=true) to apply.
